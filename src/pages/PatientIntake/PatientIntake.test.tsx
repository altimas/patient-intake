describe("Patient Intake Data Layer", () => {
  it.todo(
    "should be able to go through the whole flow with correct selections"
  );
  it.todo(
    "should show correct error states when hitting the Continue button and doesnt progress"
  );
});

export {};
